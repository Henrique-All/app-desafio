<div class="box">

<h1>Usuários</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to "#{user.name} (#{user.email})", user_consents_path(user) %></li>
  <% end %>
</ul>

<br>
<%= link_to "Criar novo usuário", new_user_path %>
</div>